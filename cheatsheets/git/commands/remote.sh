git remote show remote_name #show information about the remote (what branches, which branches are tracked, up to date, etc)
git remote set-head -d #deletes the useless origin/HEAD ref
git remote rename origin #rename remote repository
